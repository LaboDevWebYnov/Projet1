<body>
    <h1>Members</h1>
    <ul>
        {% for user in users %}
            <li>{{ user }},{{ user.email }} {{ user.active }} addresse :
                {% for addressUser in user.address %}
                    <br>
                    {{ addressUser }}
                {% endfor %}
            </li>
        {% endfor %}
    </ul>
</body>
